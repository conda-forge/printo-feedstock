schema_version: 1

context:
  name: printo
  version: "0.0.17"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/printo-${{ version }}.tar.gz
  sha256: 87b5a2ce79cacb669c4e5f28c7cb5c54f1f8866a4fdde742d6db258f426d920a

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools ==68.0.0
    - pip
  run:
    - python >=${{ python_min }}
    - sigmatch >=0.0.8
    - getsources >=0.0.3

tests:
  - python:
      imports:
        - printo
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: Print objects with data beautifully
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/pomponchik/printo

extra:
  recipe-maintainers:
    - thewchan
